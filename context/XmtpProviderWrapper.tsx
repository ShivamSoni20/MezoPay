"use client";

import { ReactNode } from "react";

export function XmtpProviderWrapper({ children }: { children: ReactNode }) {
  // Placeholder wrapper – can be extended with actual XMTP provider logic.
  return <>{children}</>;
}
